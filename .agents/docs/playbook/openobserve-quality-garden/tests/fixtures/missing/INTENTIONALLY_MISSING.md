# Intentionally missing quality reports

This fixture deliberately omits every producer metadata and report artifact.
The marker is outside `quality-reports/` so fixed-path collection tests remain
unchanged while Git retains the fixture directory.
