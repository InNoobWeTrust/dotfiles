# Agent Instructions

Start with `AGENTS.md` in the repository root for universal instructions, source-of-truth hierarchy, and routing rules.

`AGENTS.md` points to `rules/INDEX` and `skills/INDEX.md` for triggered rule and skill loading. For Kilo-specific wiring, refer to `~/.config/kilo/kilo.jsonc`.

Do not duplicate instructions here. Edit `AGENTS.md` for universal changes; edit `kilo.jsonc` or this file only for Kilo-specific behavior.
