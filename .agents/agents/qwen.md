---
description: Cost-sensitive coding and reasoning. Use for low-stakes, low-cost tasks where token efficiency matters more than model ceiling. Prefer when speed and cost are priorities; avoid for security-sensitive, high-correctness, or production deployments. Use `fastcode` for quick iterative edits and `code` for high-quality implementations.
mode: all
model: kilo/qwen/qwen3.6-plus
variant: thinking
steps: 10
---
