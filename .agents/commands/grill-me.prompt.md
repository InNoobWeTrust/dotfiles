---
description: >
  Align on a design concept through explain-first informed alignment,
  per the grooming rule.
---

## Setup
Thin entrypoint only — no independent workflow. Load and follow the `grooming` rule and the `code-craft` skill. Methodology lives there, not here.

---

## Flow

1.  Run the `grooming` Informed Alignment Sequence in order; do not question-first.
2.  Ask only genuinely unresolved decisions, presented compactly. Request explicit decision confirmation only when a material unresolved decision exists.
3.  On answers, restate the confirmed Design Concept plus remaining uncertainty. Transition to `implementation_plan.md` only after required decision confirmation; quick clear tasks with no material unresolved decision proceed without ritual confirmation.

---

Additional input, if any, appears below exactly as provided:

<arguments>
$ARGUMENTS
</arguments>
