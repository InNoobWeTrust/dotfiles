autocomplete_nvm=false

# Add ~/.local/bin to PATH if it's not there
[[ ":$PATH:" != *":$HOME/.local/bin:"* ]] && [[ -d $HOME/.local/bin ]] && export PATH="$HOME/.local/bin:$PATH"

# Set neovim as default editor
if [[ $(which nvim) ]]; then
    export EDITOR="$(which nvim)"
    export VISUAL="$(which nvim)"
else
    export EDITOR=/usr/bin/nano
fi

# Add Oni to PATH
[[ -d $HOME/Oni ]] && [[ ":$PATH:" != *":$HOME/Oni:"* ]] && export PATH="$HOME/Oni:$PATH"

# Add flutter to PATH
[[ -d $HOME/flutter/bin ]] && [[ ":$PATH:" != *":$HOME/flutter/bin:"* ]] && export PATH="$HOME/flutter/bin:$PATH"

# Add dart-sdk to PATH
[[ -d $HOME/dart-sdk/bin ]] && [[ ":$PATH:" != *":$HOME/dart-sdk/bin:"* ]] && export PATH="$HOME/dart-sdk/bin:$PATH"

# Add pub cache to PATH
[[ -d $HOME/.pub-cache/bin ]] && [[ ":$PATH:" != *":$HOME/.pub-cache/bin:"* ]] && export PATH="$HOME/.pub-cache/bin:$PATH"

# Add Go to PATH
[[ -d $HOME/.local/go/bin ]] && [[ ":$PATH:" != *"$HOME/.local/go/bin:"* ]] && export PATH="$HOME/.local/go/bin:$PATH" && export GOPATH="$HOME/.go/bin"

# Add GOPATH to PATH
[[ ! -z $GOPATH ]] && export PATH="$GOPATH:$PATH"

# Add cargo to PATH
[[ -d $HOME/.cargo/bin ]] && [[ ":$PATH:" != *":$HOME/.cargo/bin:"* ]] && export PATH="$HOME/.cargo/bin:$PATH"

# Add Miniconda3 to PATH
[[ -d $HOME/miniconda3/bin ]] && [[ ":$PATH:" != *":$HOME/miniconda3/bin:"* ]] && export PATH="$HOME/miniconda3/bin:$PATH"

# Add pyenv to PATH
[[ -d $HOME/.pyenv ]] && [[ ":$PATH:" != *":$HOME/.pyenv/bin:"* ]] && export PATH="$HOME/.pyenv/bin:$PATH" && eval "$(pyenv init -)" && eval "$(pyenv virtualenv-init -)"

# Activate nvm
if [ -d $HOME/.nvm ]; then
    export NVM_DIR="$HOME/.nvm"
    [[ -s "$NVM_DIR/nvm.sh" ]] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
    [[ -n $autocomplete_nvm ]] && [[ -s "$NVM_DIR/bash_completion" ]] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
fi
